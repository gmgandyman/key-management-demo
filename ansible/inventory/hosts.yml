# See plans/postgres-seed-playbook.md §2 for what goes here:
# one host, `postgres`, with ansible_host set to the compose service name
# (Docker's embedded DNS resolves it — same name the README already SSHes to).
database:
  hosts:
    postgres: